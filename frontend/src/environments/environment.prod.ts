// TODO: confirmar a URL definitiva do backend após o deploy no Render
// (o nome do serviço em render.yaml é "logitrackpro", mas a URL final
// depende do slug efetivamente atribuído pela plataforma).
export const environment = {
  production: true,
  apiUrl: 'https://logitrackpro.onrender.com/api',
};
